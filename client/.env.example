# Base URL of the FastAPI backend.
# Leave empty in local dev: requests stay relative and the Vite proxy in
# vite.config.ts forwards them to http://localhost:8000.
# In production set this to the deployed backend, e.g.
#   VITE_API_URL=https://trade-superstars-api.onrender.com
VITE_API_URL=
